name: Build and deploy on server

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3
    - name: Copy project to server
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.REMOTE_HOST }}
        username: ${{ secrets.REMOTE_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        source: "."
        target: ${{ secrets.REMOTE_PATH }}
        rm: true

    - name: Build and run app via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.REMOTE_HOST }}
        username: ${{ secrets.REMOTE_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: cd ${{ secrets.REMOTE_PATH }}
                echo "WHOAMI $(whoami)"
#                echo "Current dir $(pwd)"
#                ls -l
#                echo "Checking Maven..."
#                which mvn || echo "Maven not found"
#                mvn -v || echo "Maven version not found"
#
#                echo "Building project..."
#                mvn clean package -DskipTests || { echo 'Build failed'; exit 1; }
#
#                echo "Build complete. Listing target/:"
#                ls -l target